<!DOCTYPE html>
<html>
<head>
  <title>Proto</title>

  <%= stylesheet_link_tag :all %>
 <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tags %>
</head>
<body>
    <ul class="right">
      <li class="active">
        <% if user_signed_in? %>
        <td><%= link_to 'Sign Out', destroy_user_session_path, method: :delete %></td>
        <% else %>
         <td><%= link_to 'Sign in', user_session_path %></td>
        <% end %>
        
      </li>
  </ul>
      <p>
        <b><% if user_signed_in? %>
  <%= link_to "You are  logged in as #{current_user.email}", edit_user_registration_path %></b>
<% end %>
<%= yield %>

</body>
</html>
